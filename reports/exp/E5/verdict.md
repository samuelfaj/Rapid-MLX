# Verdict — E5 chunked_prefill_default_on

## Status
BLOCKED — --chunked-prefill-tokens only on serve_parser, not bench_parser. Same limitation as E3. Can't benchmark via bench mode.

## Next
E8 — ngram_K_per_workload (testable)
